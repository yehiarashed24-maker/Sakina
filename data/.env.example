# Example env file. Do NOT commit secrets.
# If using an external embedding API (OpenAI, etc.), set API keys here.
EMBEDDING_API_KEY=
EMBEDDING_MODEL=

# Optional: override chroma persist directory
CHROMA_PERSIST_DIRECTORY=data/vector_store
